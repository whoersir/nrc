# NRC项目开发沙盒环境
# 使用Node.js 18 + Next.js完整开发环境

FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 安装系统依赖
RUN apk add --no-cache \
    git \
    bash \
    curl

# 复制package文件
COPY package*.json ./

# 安装npm依赖（使用国内镜像加速）
RUN npm config set registry https://registry.npmmirror.com && \
    npm install

# 复制项目文件
COPY . .

# 暴露开发端口
EXPOSE 3000 5000

# 开发服务器启动命令
CMD ["npm", "run", "dev"]
